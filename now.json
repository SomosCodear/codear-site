{
  "version": 2,
  "name": "codear-site",
  "alias": ["codear.org"],
  "builds": [
    {
      "src": "package.json",
      "use": "@now/static-build",
      "config": { "distDir": "out" }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/([^/]+)", "dest": "/$1.html" }
  ]
}
